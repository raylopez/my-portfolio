import { SmoothClick } from './smooth-click';

describe('SmoothClick', () => {
  it('should create an instance', () => {
    const directive = new SmoothClick();
    expect(directive).toBeTruthy();
  });
});
